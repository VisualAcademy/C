#include <stdio.h>

// max.h 파일로 이동
//int max(int first, int second);

#include "max.h"

int main()
{
    printf("MAX: %d\n", max(3, 5));

    return 0;
}

// max.c 파일로 이동
//int max(int first, int second)
//{
//    return first > second ? first : second;
//}
